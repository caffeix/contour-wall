# ContourWall Desktop Launcher

## Setup
- Install dependencies: `npm install`
- Run the launcher: `npm start`

## Notes
- The launcher opens games in a new terminal window.
- Set `PYTHON_EXECUTABLE` if you need a specific interpreter.
